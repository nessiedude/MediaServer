<p>
	<strong>Name:</strong>
	<%= @artist.name %>
</p>

<p>
	<strong>Albums:</strong><br/>
	<% @artist.albums.each {|album| %>
		<%= link_to album.title,album %><br/>
	<% } %>
</p>

<p>
	<strong>Tracks:</strong><br/>
	<% @artist.tracks.each {|track| %>
		<%= link_to track.title,track %><br/>
	<% } %>
</p>

<%= link_to 'Back', artists_path %>
